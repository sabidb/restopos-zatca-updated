{
  "name": "restopos-rules-test",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Firestore security-rules tests. Kept out of the app's package.json so the frontend build stays lean.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-restopos --config ../firebase.emulator.json \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.0.0"
  }
}
