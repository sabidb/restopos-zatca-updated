{
  "name": "restopos-functions",
  "type": "module",
  "main": "index.js",
  "engines": { "node": "22" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:emulator": "firebase emulators:exec --project restopos-db --only functions,firestore,auth \"node --test test/*.test.mjs\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
